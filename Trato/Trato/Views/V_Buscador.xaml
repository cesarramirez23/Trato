<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Trato.Views.V_Buscador">
    <ContentPage.Content>
        <AbsoluteLayout  AbsoluteLayout.LayoutBounds="0,0,1,1">
            <!--Orientation="Vertical">-->
            <StackLayout VerticalOptions="Start" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">
                <Button Clicked="Fn_Filtro"  Text="Filtro" Style="{StaticResource Buton}"></Button>
                <!--<Picker Title="Filtro"  HorizontalOptions="StartAndExpand" x:Name="v_filtro" SelectedIndexChanged="Fn_Filtro">
                    <Picker.Items>
                        <x:String>Nombre</x:String>
                        <x:String>Especialidad</x:String>
                        <x:String>Ciudad</x:String>
                        <x:String>Todos</x:String>
                    </Picker.Items>
                </Picker>-->

                <ListView  x:Name="v_lista" RowHeight="150" SeparatorColor="Gray" IsPullToRefreshEnabled="True" ItemSelected="Fn_Select" Refreshing="Fn_Refresh">                
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <!--https://stackoverflow.com/questions/49373982/access-views-inside-a-datatemplate-at-runtime-in-xamarin-forms -->
                                <StackLayout Orientation="Horizontal" HorizontalOptions="Fill">
                                    <StackLayout Orientation="Vertical" VerticalOptions="StartAndExpand" HorizontalOptions="CenterAndExpand">
                                      
                                        <Label Text="{Binding v_Apellido}"/>
                                        <Label Text="{Binding v_Nombre}" />
                                        <Label Text="{Binding v_Especialidad}" />
                                        <!--<Label Text="{Binding v_Domicilio}" />
                                        <Label Text="{Binding v_Info}" />-->
                                    </StackLayout>
                                    <Image HorizontalOptions="End" Source="{Binding v_img}" />
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                </StackLayout>
            <ContentView x:Name="overlay"
                           AbsoluteLayout.LayoutBounds="0, 0, 1, 1"
                           AbsoluteLayout.LayoutFlags="All"
                           IsVisible="False"
                           BackgroundColor="#C0808080"
                           Padding="10, 0">
                <StackLayout Orientation="Vertical">
                    <StackLayout Orientation="Horizontal">
                        <Button Text=" cancel" Clicked="Fn_Cancelar" HorizontalOptions="StartAndExpand" Style="{StaticResource Buton}" ></Button>
                        <Button Text=" acep" Clicked="Fn_Aceptar" HorizontalOptions="CenterAndExpand" Style="{StaticResource Buton}"></Button>
                        <Button Text="Borrar" Clicked="Fn_Cancelar" HorizontalOptions="EndAndExpand" Style="{StaticResource Buton}"></Button>
                    </StackLayout>
                            <Label HorizontalOptions="Center" Text="Especialidad"></Label>
                    <ListView x:Name="filEspc" ItemTapped="Fn_TapEspec" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextCell Text="{Binding v_texto }" TextColor="{Binding v_color}">
                                </TextCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                            
                    
                    <Label HorizontalOptions="Center" Text="CIUDAD"></Label>
                    <ListView x:Name="filCiudad" ItemTapped="Fn_TapCiu">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextCell Text="{Binding v_texto }" TextColor="{Binding v_color}">
                                </TextCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>
                <!-- Overlay -->
                    </ContentView>  
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>